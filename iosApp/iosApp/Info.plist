<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>AlarmBuddy</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>CFBundleDisplayName</key>
    <string>AlarmBuddy</string>
    <key>UILaunchScreen</key>
    <dict/>

    <!-- Barcode scanning (AVCaptureDevice access) -->
    <key>NSCameraUsageDescription</key>
    <string>AlarmBuddy uses the camera to scan a barcode you set as a way to stop your alarm.</string>

    <!-- Two uses: (1) lets an alarm that's already ringing keep playing its
         sound if the app is briefly backgrounded (e.g. the user glances at
         another app while doing the barcode/shake task), and (2) is what
         allows BackgroundKeepAlive.ios.kt's near-silent looping playback to
         keep the process alive overnight instead of getting suspended, so it
         can start ringing on its own at the target time instead of waiting
         for a notification tap. See MIGRATION_PLAN.md for what this can and
         can't do compared to Android's foreground service. -->
    <key>UIBackgroundModes</key>
    <array>
        <string>audio</string>
    </array>

    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
</dict>
</plist>
